{
  "name": "revengine",
  "version": "0.2.0",
  "private": true,
  "description": "RevEngine — automotive spec vault and comparison platform. Plain HTML/CSS/JS front end, Express + EJS server, no framework/build step.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "three": "^0.170.0"
  }
}
